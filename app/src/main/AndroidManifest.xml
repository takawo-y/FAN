<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.takawo.fan" >
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:icon="@drawable/ic_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.PlayerRegistrationActivity"/>
        <activity
            android:name=".activity.PlayerUpdateActivity"/>
        <activity
            android:name=".activity.GameActivity"/>
        <activity
            android:name=".activity.GameRegistrationActivity"/>
        <activity
            android:name=".activity.GameUpdateActivity"/>
        <activity
            android:name=".activity.GameImageRegistrationActivity"/>
    </application>

</manifest>
